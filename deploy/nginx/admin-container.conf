server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location = /health {
    access_log off;
    return 200 "ok\n";
  }

  location /admin/ {
    try_files $uri $uri/ /admin/index.html;
  }
}
