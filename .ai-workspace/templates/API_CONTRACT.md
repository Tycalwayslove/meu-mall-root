# API 契约：<接口名称>

## 基本信息

- 契约编号：
- 状态：draft
- 提供方：后端
- 消费方：`hybird-meumall`
- 适用环境：dev / test / prod
- 关联工作项：
- 关联对接说明：

## 背景

说明 H5 为什么需要该接口。

## 接口定义

- Method：`GET|POST|PUT|DELETE`
- Path：`/api/...`
- 鉴权：无 / 用户登录态 / 管理员 / 其他
- 幂等性：
- 缓存策略：
- 超时时间：

## 请求参数

### Query

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |

### Body

```json
{}
```

## 响应结构

```json
{}
```

## 字段说明

| 字段 | 类型 | 必填 | 说明 | 兼容规则 |
| --- | --- | --- | --- | --- |

## 错误码

| code | HTTP 状态 | 说明 | H5 处理方式 |
| --- | --- | --- | --- |

## H5 兜底策略

说明接口不可用、字段缺失、返回空数据、用户未登录时 H5 的处理方式。

## Mock 数据

```json
{}
```

## 兼容性要求

- 新增字段：
- 删除字段：
- 字段类型变化：
- 默认值：

## 测试方式

- 后端验证：
- H5 验证：
- 契约测试：
- 联调环境：

## 变更流程

1. 更新本契约。
2. 后端实现或调整。
3. H5 更新消费逻辑。
4. 联调验证。
5. 更新工作项和对接说明。

## 回滚方式

说明接口异常时如何回滚或降级。

